; Local dev PHP limits (used by `composer serve` / `composer dev`).
; `php artisan serve` alone does not read .user.ini or .htaccess.
upload_max_filesize = 200M
post_max_size = 210M
memory_limit = 256M
max_execution_time = 300
max_input_time = 300
